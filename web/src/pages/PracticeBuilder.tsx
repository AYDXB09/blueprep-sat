import { PageStub } from '../components/PageStub';

export function PracticeBuilder() {
  return (
    <PageStub
      title="Ad-hoc Practice Builder"
      path="/practice/new"
      mockupNote="Validated as a real working HTML mockup already (blueprep_builder_mock.html) — mixed-subject split, live steppers, quick-pick presets, pool-scarcity warning. Not yet ported into a React component."
    />
  );
}
