import { PageStub } from '../components/PageStub';

export function Progress() {
  return (
    <PageStub
      title="Progress & Score Tracking"
      path="/progress"
      mockupNote="Long-horizon view across every practice_session and question_attempt — score trend, accuracy by domain, rushed/slow-right/slow-wrong pacing breakdown."
    />
  );
}
