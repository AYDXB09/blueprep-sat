import { PageStub } from '../components/PageStub';

export function FullTestSetup() {
  return (
    <PageStub
      title="Full Test Setup"
      path="/test/new"
      mockupNote="Module 1 → Module 2 Tier 1/Tier 2 adaptive routing, drawn from session_modules and tier_difficulty_profiles. The routing threshold is an unverified approximation — say so in the UI, not just in code comments."
    />
  );
}
